services:
  logpheus:
    build:
      context: .
    env_file: .env
    restart: always
    ports:
      - 53814:3000
    volumes:
      - cache:/usr/src/app/cache
    healthcheck:
      test: ["CMD", "curl", "-f", "http://localhost:3000/healthcheck"]
      interval: 30s
      timeout: 10s
      retries: 3
      start_period: 10s

volumes:
  cache: